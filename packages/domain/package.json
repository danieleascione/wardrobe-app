{
  "name": "@pocketwardrobe/domain",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "tsc --build"
  },
  "devDependencies": {
    "@stryker-mutator/core": "8.2.6",
    "@stryker-mutator/vitest-runner": "8.2.6",
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
